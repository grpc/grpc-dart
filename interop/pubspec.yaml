name: interop
description: Dart gRPC interoperability test suite.
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  args: ^2.0.0
  async: ^2.2.0
  collection: ^1.14.11
  grpc:
    path: ../
  protobuf: ^4.0.0

dev_dependencies:
  lints: '>=5.0.0 < 7.0.0'
  test: ^1.16.0
