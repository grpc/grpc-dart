name: helloworld
description: Dart gRPC sample client and server.
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  async: ^2.2.0
  grpc:
    path: ../../
  protobuf: ^4.0.0

dev_dependencies:
  lints: '>=5.0.0 < 7.0.0'
