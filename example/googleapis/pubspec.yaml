name: googleapis
description: Dart gRPC client sample for Google APIs
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  async: ^2.2.0
  fixnum:
  grpc:
    path: ../../
  protobuf: ^4.0.0

dev_dependencies:
  lints: '>=5.0.0 < 7.0.0'
  test: ^1.6.4
