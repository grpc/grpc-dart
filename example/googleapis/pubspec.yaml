name: googleapis
description: Dart gRPC client sample for Google APIs
publish_to: none

environment:
  sdk: '>=2.16.1 <3.0.0'

dependencies:
  async: ^2.8.2
  grpc:
    path: ../../
  protobuf: ^2.0.1

dev_dependencies:
  test: ^1.20.1
