name: grpc_web
description: Dart gRPC-Web sample client
publish_to: none

environment:
  sdk: '>=2.16.1 <3.0.0'

dependencies:
  grpc:
    path: ../../
  protobuf: ^2.0.1

dev_dependencies:
  build_runner: ^2.1.7
  build_web_compilers: ^3.2.2
